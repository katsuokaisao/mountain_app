<div class="comment-list">
  <div class="comment-list">
    <% if comment.user.profile && comment.user.profile.avatar.attached? %>
        <%= image_tag comment.user.profile.avatar.variant(resize_to_fill: [40, 40])  %>
    <% end %>
    <p><%= comment.user.username %></p>
    <p><%= comment.created_at  %></p>
  </div>
</div>