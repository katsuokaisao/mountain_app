<% @entries.each do |entry|%>
  <% other_entry = Entry.where(room_id: entry.room_id).where.not(user_id: current_user.id) %>
  <%= other_entry.first.user.username if other_entry.present? %>さんとの
  <%= link_to 'チャットルームへ', room_path(entry.room_id) %>
<% end %>