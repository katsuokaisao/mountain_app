<%= form_with model: @user.active_relationships.find_by(followed_id: user.id), method: :delete, class: 'unfollow-form' do |f| %>
  <div><%= hidden_field_tag :followed_id, user.id %></div>
  <%= f.submit "フォロー中" %>
<% end %>