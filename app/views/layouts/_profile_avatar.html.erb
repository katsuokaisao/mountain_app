<% if current_user.profile && current_user.profile.avatar.attached? %>
  <%= image_tag current_user.profile.avatar.variant(resize_to_fill: [a, b]).service_url %>
<% else %>
  <%= image_tag 'stockfoto_12670415.jpg'%>
<% end %>